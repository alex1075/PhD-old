```bash
$ python -c 'from code.convert import convert; convert()'
$ python -c 'from code.convert import resizeAllJpg; resizeAllJpg()'
$ python -c 'from code.convert import convertVideoToImage; convertVideoToImage()'
$ python -c "from code.cycleGan import cycleGAN as cg; cg = cg(); cg.train(100, 2)”
```